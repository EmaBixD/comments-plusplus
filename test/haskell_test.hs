module Main where

-- TODO: Implementare il parser
parseString :: String -> String
parseString input = "risultato" -- FIXME: stub implementation

{- NOTE: Commento multiriga in Haskell -}
my_fun x =
    if not (null (filter (== 'c') note)) -- "note" e "not" etc falsi positivi
      then True
      else False
